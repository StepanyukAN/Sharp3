Есть таблица Users. Столбцы в ней - Id, Name. Написать SQL-запрос, который выведет имена
пользователей, начинающиеся на 'A', и которые встречаются в таблице более одного раза, и
их количество

SELECT Name, COUNT(*) FROM Users WHERE Name LIKE "A%" AND COUNT(Name)>1

Думаю, как-то так.